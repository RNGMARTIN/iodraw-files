```mermaid
flowchart TD
    "数据输入(评估对象原始数据)" --> "标准评估检查(是否达标？)"
    "标准评估检查(是否达标？)" -->|Yes| "传统评估通过(输出合格结果)"
    "标准评估检查(是否达标？)" -->|No| "激活替代函数(选择替代方式)"
    "激活替代函数(选择替代方式)" --> "数据处理与转换(调整数据适应替代)"
    "数据处理与转换(调整数据适应替代)" --> "权限验证(评估权限模块)"
    "权限验证(评估权限模块)" --> "替代评估执行(补考/减刑等)"
    "替代评估执行(补考/减刑等)" --> "反馈记录(反馈机制模块)"
    "反馈记录(反馈机制模块)" --> "输出最终结果"
    
```